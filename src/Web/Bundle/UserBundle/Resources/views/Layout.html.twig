{% extends '::Base.html.twig' %}

{% block body %}
    <div class="blog-masthead">
        <div class="container">
            <nav class="blog-nav">
                <a class="blog-nav-item{% if app.session.get('controller') == 'rtc' %} active{% endif %}" href="{{ path('rooms') }}">Pokoje</a>
                <a class="blog-nav-item{% if app.session.get('controller') == 'room' %} active{% endif %}" href="{{ path('admin_rooms') }}">Zarządzaj pokojami</a>
                <a class="blog-nav-item" href="{{ path('fos_user_security_logout') }}">Wyloguj</a>
            </nav>
        </div>
    </div>

    <div class="container">
        <div class="blog-header">
            <h1 class="blog-title">WebRTC</h1>

            <p class="lead blog-description">Michał jest pedałem i nie zmieni tego opisu.</p>
        </div>

        <div class="row">
            {% block content %}{% endblock %}
        </div>
    </div>

    <div class="blog-footer">
        <p>Copyright &copy; by Michał Drabiński.</p>

        <p>
            <a href="#">Back to top</a>
        </p>
    </div>
{% endblock %}